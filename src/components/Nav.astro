---
---

<header class="nav-header">
  <nav class="nav-inner">
    <a href="#top" class="nav-home">
      <span style="color: var(--color-accent);">[</span>sf13<span style="color: var(--color-warn);">@</span>dev<span style="color: var(--color-accent);">]</span>&gt; _
    </a>
    <div class="nav-links">
      <a href="#about">about</a>
      <a href="#experience">exp</a>
      <a href="#skills">skills</a>
      <a href="#contact">contact</a>
    </div>
  </nav>
</header>

<style>
  .nav-header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    background: rgba(10, 11, 14, 0.92);
    border-bottom: 1px solid var(--color-border);
  }
  .nav-inner {
    max-width: 56rem;
    margin: 0 auto;
    padding: 0 1rem;
    height: 2.5rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-size: 13px;
  }
  .nav-home {
    color: var(--color-fg);
  }
  .nav-home:hover {
    background: transparent;
    color: var(--color-accent);
  }
  .nav-links {
    display: flex;
    gap: 1.25rem;
  }
  .nav-links a {
    color: var(--color-fg-dim);
    font-size: 12px;
    letter-spacing: 1px;
    text-transform: uppercase;
  }
  .nav-links a:hover {
    color: var(--color-accent);
    background: transparent;
  }
</style>
